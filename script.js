const gameBoard = (function () {
  let gameBoardArray = Array(9).fill(null);
  return {
    gameBoardArray,
  };
})();

const displayController = (function () {
  return {

  };
})();

const Player = (name) => {
  return {

  };
};